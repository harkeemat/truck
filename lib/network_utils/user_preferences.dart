import 'dart:convert';

import 'package:truck/model/user.dart';
import 'package:truck/network_utils/api.dart';

class UserPreferences {
  // Future<User> getuserid(id) async {
  // var res = await Network().authData(id, '/get_user');
  // final jsonresponse = json.decode(res.body);
  // return jsonresponse['data'][0].isNotEmpty
  // ? User.fromMap(jsonresponse['data'][0])
  // : Null;
  // }
}
